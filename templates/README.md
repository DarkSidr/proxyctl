# Templates

Runtime templates used by `proxyctl` builders:

- `templates/caddy/Caddyfile.tmpl` - default Caddy reverse proxy template.
- `templates/decoy-site/` - static decoy site assets copied into runtime layout.
